#################################################
#
#   Forms
#
#################################################

- id: form_element
  name: |
    `<form>`
  patterns:
    - form [e]
    - "<form> [e]"

- id: select_element
  name: |
    `<select>`
  patterns:
    - <select>
    - <select/>
    - custom select
    - select [e]
    - selects [e]
  tags:
    - html_functionality_features
  descriptionIssues: |
    Pain points related to the `<select>` element.
  parentId: forms_issues

- id: select_multiple_element
  name: |
    `<select multiple>`
  patterns:
    - multi, select [l]
  tags:
    - html_functionality_features

- id: input_type_date
  name: |
    `<input type="date">`
  descriptionIssues: >
    Pain points related to `<input type="date">` and its associated picker UI.
  patterns:
    - input type="date"
    - date, input [l]
    - date, picker [l]
    - calendar
    - calender
    - date
  parentId: pickers
  tags:
    - forms_pain_points

- id: input_type_time
  idAliases:
    - time_picker
  name: |
    `<input type="time">`
  patterns:
    - input type="time"
    - time, input [l]
    - time, picker [l]
  descriptionIssues: |
    Pain points related to `<input type="time">` and its associated picker UI.
  parentId: pickers
  tags:
    - html_missing_elements
    - forms_pain_points

- id: input_type_datetime_locale
  name: |
    `<input type="datetime-local">`
  patterns:
    - datetime
    - date\/time

- id: input_type_month
  name: |
    `<input type="month">`
  patterns:
    - <input type="month"
    - <input type="month"
    - input month

- id: input_type_week
  name: |
    `<input type="week">`

- id: input_type_color
  webFeaturesId: input-color
  name: |
    `<input type="color">`
  description: |
    Creates a color picker from which users can choose a color value.
  tags:
    - html_interoperability_features
  patterns:
    - color( |-)?picker
  mdn: /en-US/docs/Web/HTML/Reference/Elements/input/color
  resources:
    - url: https://html.spec.whatwg.org/multipage/input.html#color-state-(type=color)
      title: HTML Standard

- id: input_type_color_colospace
  name: |
    `<input type="color" colorspace="display-p3">`
  description: |
    Creates a color picker which allows the user to select a wide-gamut (P3) color.
  tags:
    - html_interoperability_features
  resources:
    - url: https://webkit.org/blog/16900/p3-and-alpha-color-pickers/
      title: Add wide gamut P3 and alpha transparency to your color picker in HTML | WebKit
    - url: https://html.spec.whatwg.org/multipage/input.html#attr-input-colorspace
      title: HTML Standard

- id: input_type_color_alpha
  name: |
    `<input type="color" alpha>`
  description: |
    Creates a color picker which allows the user to select a semi-transparent color.
  tags:
    - html_interoperability_features
  resources:
    - url: https://webkit.org/blog/16900/p3-and-alpha-color-pickers/
      title: Add wide gamut P3 and alpha transparency to your color picker in HTML | WebKit
    - url: https://html.spec.whatwg.org/multipage/input.html#attr-input-alpha
      title: HTML Standard

- id: input_type_color_list
  name: |
    `<input type="color" list>` for swatches

- id: input_type_email
  name: |
    `<input type="email">`

- id: input_type_tel
  name: |
    `<input type="tel">`

- id: input_type_url
  name: |
    `<input type="url">`

- id: input_type_month
  name: |
    `<input type="month">`

- id: input_type_number
  name: |
    `<input type="number">`
  patterns:
    - number

- id: input_type_range
  name: |
    `<input type="range">`
  tags:
    - html_missing_elements
  descriptionIssues: |
    Pain points related to `<input type="range">`.
  parentId: forms_issues

- id: input_type_range_list
  name: |
    `<input type="range" list>` for ticks

- id: input_type_file
  name: |
    `<input type="file">`
  patterns:
    - file

- id: output_element
  name: |
    `<output>`

- id: required_attribute
  name: |
    `required` attribute

- id: pattern_attribute
  name: |
    `pattern` attribute
  descriptionIssues: |
    Issues related to the `pattern` attribute for validation.
  parentId: form_validation_issues

- id: input_set_custom_validity
  name: |
    `input.setCustomValidity()`

- id: input_report_validity
  name: |
    `input.reportValidity()`

- id: input_check_validity
  name: |
    `input.checkValidity()`

- id: invalid_event
  name: |
    `invalid` event

- id: invalid_pseudo_class
  name: |
    `:invalid` CSS pseudo-class

- id: valid_invalid_pseudo_class
  parentId: validation
  name: |
    `:invalid`/`:valid` CSS pseudo-classes
  # note: whole-word matching ([b]) breaks when using : character in a string
  patterns:
    - ":valid [p]"
    - ":invalid [p]"
    - ":user-invalid [p]"
    - ":user-valid [p]"
    - valid, pseudo [l]
    - invalid, pseudo [l]
  tags:
    - forms_pain_points
    - css_interoperability_features

- id: edit_context
  webFeaturesId: edit-context
  name: |
    `EditContext`
  description: |
    The EditContext interface represents the text edit context of an element that was made editable by using the EditContext API.
  mdn: en-US/docs/Web/API/EditContext
  example:
    language: js
    code: |
      const canvas = document.createElement("canvas");
      const editContext = new EditContext();
      canvas.editContext = editContext;

- id: caret_position_from_point
  webFeaturesId: document-caretpositionfrompoint
  name: |
    `caretPositionFromPoint`
  description: |
    The caretPositionFromPoint() method returns the caret's character offset.
  example:
    language: js
    code: |-
      const range = document.caretPositionFromPoint(e.clientX, e.clientY);
      const textNode = range.offsetNode;
      const offset = range.offset;

- id: input_checkbox_switch
  name: |
    Switch Control
  name2: |
    `<input type="checkbox" switch>`
  description: |
    A way to render a checkbox as a switch.
  descriptionIssues: >
    The lack of a native switch or toggle control, forcing developers to build
    one from a checkbox.
  resources:
    - url: https://webkit.org/blog/15054/an-html-switch-control/
      title: An HTML Switch Control | WebKit
    - url: https://www.smashingmagazine.com/2024/05/switching-it-up-html-latest-control/
      title: Switching It Up With HTML’s Latest Control — Smashing Magazine

- id: input_element
  name: |
    `<input>`
  descriptionIssues: >
    General pain points with the `<input>` element or for input types not
    covered by more specific codes.
  parentId: forms_issues

- id: color_picker
  name: |
    `<input type="color">`
  patterns:
    - color, picker [l]
    - input, color [l]
  tags:
    - html_missing_elements
    - html_missing_features
  descriptionIssues: |
    Pain points related to `<input type="color">` and its associated picker UI.
  parentId: pickers

- id: date_range_picker
  name: |
    Date Range Picker
  description: |
    A native control for selecting a start and an end date.
  parentId: input_type_date
  tags:
    - html_missing_elements

- id: select_element_multiselect
  name: |
    `<select multiple>`
  descriptionIssues: |
    Issues with the user experience and appearance of `<select multiple>`.
  parentId: select_element
  patterns:
    - multi( |-)?select
    - select, multiple [l]

- id: textarea_element
  name: |
    `<textarea>`
  descriptionIssues: |
    Pain points specific to the `<textarea>` element.
  parentId: forms_issues
  tags:
    - forms_pain_points
  patterns:
    - text(-| )?area

- id: autocomplete
  name: Autocomplete
  description: Autocomplete, comboxbox, typeahead, etc. inputs
  descriptionIssues: |
    Pain points with the `autocomplete` attribute and browser autofill behavior.
  parentId: forms_issues
  patterns:
    - combo( |-)?box
    - autocomplete
    - typeahead
    - autoco [p]
  tags:
    - html_missing_elements
    - html_functionality_features
    - css_missing_features
    - forms_pain_points

- id: date_time_picker
  name: Date & Time Picker
  tags:
    - html_missing_elements

- id: input_type_checkbox
  name: |
    `<input type="checkbox">`
  descriptionIssues: |
    Pain points specific to `<input type="checkbox">`.
  parentId: forms_issues
  mdn: en-US/docs/Web/HTML/Reference/Elements/input/checkbox
  patterns:
    - checkbox [p]

- id: input_type_radio
  name: |
    `<input type="radio">`
  descriptionIssues: |
    Pain points specific to `<input type="radio">`.
  parentId: forms_issues
  mdn: en-US/docs/Web/HTML/Reference/Elements/input/radio
  patterns:
    - radio

- id: fieldset_element
  name: |
    `<fieldset>`
  descriptionIssues: >
    Pain points related to styling and using the `<fieldset>` and `<legend>`
    elements.
  parentId: forms_issues
  patterns:
    - fieldset

- id: input_type_number
  idAliases:
    - number_input
  name: |
    `<input type="number">`
  descriptionIssues: >
    Pain points specific to `<input type="number">`, including styling stepper
    buttons and inconsistent behavior.
  parentId: forms_issues

- id: input_type_file
  idAliases:
    - file_input
  name: |
    `<input type="file">`
  description: >
    Pain points specific to `<input type="file">`, especially around styling the
    button and displaying selected files.
  parentId: forms_issues
